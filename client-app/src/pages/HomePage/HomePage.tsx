import React from "react";
import styles from "./homePage.module.css";

const HomePage = () => {
  return (
    <div className={`${styles.container} container`}>
      Oops!!
      <br />
      Home page is temporary empty
    </div>
  );
};

export default HomePage;
